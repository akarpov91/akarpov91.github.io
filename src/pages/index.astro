---
import Layout from '../layouts/Layout.astro';
import { nav } from '../lib/constants';
//console.log('nav', nav);
---

<Layout title="Alex Karpov">
	<main class="flex justify-center items-center h-screen">
		<div class="w-3/4"><span class="font-extrabold text-transparent text-4xl bg-clip-text bg-gradient-to-r from-pink-400 to-purple-600">Alex Karpov!</span>
			<p class="pt-2">
				I'm a JavaScript developer based in the Chicago, IL. I'm passionate about building web applications and solving interesting problems. I enjoy working with frameworks such as <a href="" class="text-purple-600 font-semibold underline underline-offset-4">React</a> and <a href="" class="text-purple-600 font-semibold underline underline-offset-4">NextJS</a>.
			</p>
			<div class="flex flex-row justify-end pt-4">
				{nav.map(({ title, href }) => (
						<div class="text-2xl text-black text-right font-extrabold pl-11 hover:text-purple-600 hover:underline hover:underline-offset-8"><a href={href}>{title}</a></div>
				))}
			</div>
		</div>
	</main>
</Layout>

